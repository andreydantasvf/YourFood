import styled from "styled-components";

export const Container = styled.button`
    color: white;
    background: none;
    border: none;
    font-size: 2.4rem;
    display: flex;
    align-items: center;
    gap: .8rem;
    > svg {
        font-size: 2.4rem;
        color: white;
    }
`;
